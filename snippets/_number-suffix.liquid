{%- assign number = number | to_i | floor -%}
{%- assign remainder = number | modulo: 10 -%}

{%- if number > 3 and number < 21 -%}
	th
{%- else -%}
	{%- case remainder -%}
		{%- when 1 -%}
			st
		{%- when 2 -%}
			nd
		{%- when 3 -%}
			rd
		{%- else -%}
			th
	{%- endcase -%}
{%- endif -%}